package com.ingbank.banking.service;

import static org.junit.Assert.assertNotNull;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.junit.MockitoJUnitRunner;
import org.springframework.beans.factory.annotation.Autowired;

import com.ingbank.banking.entity.Customer;
import com.ingbank.banking.exception.ApplicationException;
import com.ingbank.banking.service.impl.CustomerServiceImpl;

@RunWith(MockitoJUnitRunner.class)
public class CustomerServiceTest
{
	/*
	 * @Autowired CustomerService customerService;
	 * 
	 * @Mock CustomerServiceImpl customerSericeImpl;
	 * 
	 * Customer customer=new Customer();
	 * 
	 * @Test public void testAddCustomer() throws ApplicationException {
	 * Mockito.when(customerSericeImpl.addCustomer(Mockito.anyObject())).thenReturn(
	 * customer); assertNotNull(customer);
	 * 
	 * }
	 */
	
	
	
}